-- single line comment -- double hyphen

SELECT column_names
FROM table_name
WHERE Coloumn= row_value
ORDER BY Column_name,column_name DESC or by default asceding
-- first priority for order is given to the 1st column if same values then only it verifies by 2nd column name

